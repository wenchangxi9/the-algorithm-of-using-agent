# Secrets and local credentials
secrets/
.env
*.env
*api_key*
*secret*

# Python/runtime cache
__pycache__/
*.pyc
.ipynb_checkpoints/

# Local/raw external data
/data/
raw_data/
*.tsv.gz
*.parquet

# Bulky raw API response dumps; parsed CSV summaries are kept instead.
posts_tikhub.jsonl
agent_votes.raw.jsonl
